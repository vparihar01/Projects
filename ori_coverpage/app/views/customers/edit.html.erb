<%= render :partial => 'shared/admin_edit_actions', :locals => {:record => @customer} %>

<h1><%= calculate_page_heading %></h1>

<%= form_for @customer do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>  
  <%= submit_or_cancel customer_url(@customer) %>   
<% end %>

