<tr id="<%= dom_id(assignment) %>">
  <td><%= link_to assignment.link_title, admin_link_path(assignment) %></td>
  <td class="center"><%= link_to( image_tag( 'buttons/admin/trash.gif', :alt => '' ), delete_link_admin_product_path(:id => @product.id, :link_id => assignment.id), :remote => true, :method => :delete, :confirm => 'Are you sure you wish to delete this record?', :title => 'Delete' ) %></td>
</tr>
