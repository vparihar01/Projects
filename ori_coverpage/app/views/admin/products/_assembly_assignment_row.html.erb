<tr id="<%= dom_id(assignment) %>">
  <td><%= link_to product.name_for_dropdown, edit_admin_product_path(product) %></td>
  <td class="right"><%= number_to_currency(product.default_price_list) %></td>
  <td class="right"><%= number_to_currency(product.default_price) %></td>
  <td class="center"><%= link_to( image_tag( 'buttons/admin/trash.gif', :alt => '' ), admin_assembly_assignment_path(assignment), :remote => true, :method => :delete, :confirm => 'Are you sure you wish to delete this record?', :title => 'Delete' ) %></td>
</tr>
