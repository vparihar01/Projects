<%= render :partial => 'shared/admin_edit_actions', :locals => {:record => @page} %>

<h1><%= calculate_page_heading %></h1>

<%= form_for @page, :url => admin_page_path(@page) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_or_cancel(admin_pages_path) %>
<% end %>

<%= render :partial => 'shared/textile_allowed' %>
