<%= render :partial => 'shared/admin_new_actions' %>

<h1><%= calculate_page_heading %></h1>

<%= form_for @teaching_guide, :url => admin_teaching_guides_path, :html => {:multipart => true} do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_or_cancel %>
<% end %>

<%= render :partial => 'shared/textile_allowed', :locals => {:fields => %w(rationale lesson)} %>
