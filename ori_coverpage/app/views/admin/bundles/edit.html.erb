<%= render :partial => 'shared/admin_edit_actions', :locals => {:record => @bundle} %>

<h1><%= calculate_page_heading %></h1>

<%= form_for @bundle, :url => admin_bundle_url(@bundle) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <%= submit_or_cancel(admin_bundles_path) %>
<% end %>
