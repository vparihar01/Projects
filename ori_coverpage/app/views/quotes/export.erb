Quantity,Product Name,ISBN,Format,Unit Price,Subtotal
<% @quote.line_items.each do |item| -%>
"<%= ([item.quantity, item.product.name, item.product_format.isbn13str, item.format, item.unit_amount, item.total_amount]).join('","').html_safe %>"
<% end -%>
