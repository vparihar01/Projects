<ul class="shortcuts">
  <%= content_tag(:li, link_to("Logout", logout_path), :class => 'first') if logged_in? %>
  <%= content_tag(:li, link_to("Login", login_path), :class => 'first') unless logged_in? %>
  <%= content_tag(:li, link_to("Register", signup_path)) unless logged_in? %>
  <%= content_tag(:li, link_to('Admin', admin_path)) if admin? %>
  <%= content_tag(:li, link_to("Wishlist", wishlists_path)) if logged_in? %>
  <%= content_tag(:li, link_to("Cart", cart_path), :class => 'last') %>
</ul>
